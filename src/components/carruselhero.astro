---
import libros from "../data/libros.json";
const slides = libros.slice(0, 9);
---

<!-- Pegado al navbar (sin espacio superior) -->
<section class="pt-0 pb-4">
  <div class="container-fluid px-0">
    <div
      id="heroCarousel"
      class="carousel slide carousel-fade bev-carousel-custom"
      data-bs-ride="carousel"
      data-bs-interval="5000"
    >
      <div class="carousel-indicators">
        {slides.map((_, i) => (
          <button
            type="button"
            data-bs-target="#heroCarousel"
            data-bs-slide-to={i}
            class={i === 0 ? "active" : ""}
            aria-current={i === 0 ? "true" : "false"}
            aria-label={`Slide ${i + 1}`}
          />
        ))}
      </div>

      <div class="carousel-inner">
        {slides.map((libro, i) => (
          <div
            class={`carousel-item bev-slide ${i === 0 ? "active" : ""}`}
            style={`background-image: url('${libro.imagen_hero || libro.imagen}');`}
          >
            <div class="bev-slide-overlay"></div>
            <div class="container h-100">
              <div class="row h-100 align-items-center justify-content-center">
                <!-- Ahora el contenido ocupa casi todo el ancho y está CENTRADO -->
                <div class="col-lg-10 col-12 bev-slide-content text-center mx-auto">

                  {libro.etiqueta === "nuevo" && (
                    <span class="bev-hero-badge bev-etiqueta-nuevo">Nuevo</span>
                  )}
                  {libro.etiqueta === "preventa" && (
                    <span class="bev-hero-badge bev-etiqueta-preventa">Próximo lanzamiento</span>
                  )}

                  <span class="bev-eyebrow">{libro.genero}</span>
                  <h1 class="bev-slide-title">{libro.titulo}</h1>
                  <p class="bev-slide-author">{libro.autor}</p>
                  <p class="bev-slide-desc">
                    {libro.descripcion ? libro.descripcion.substring(0, 120) + "..." : ""}
                  </p>

                  <div class="d-flex gap-3 flex-wrap mt-4 justify-content-center">
                    <button
                      class="btn bev-btn-gold btn-add-cart"
                      data-id={libro.id}
                      data-titulo={libro.titulo}
                      data-precio={libro.precio}
                    >
                      Agregar al carrito
                    </button>
                  </div>

                </div>
              </div>
            </div>
          </div>
        ))}
      </div>

      <button class="carousel-control-prev" type="button" data-bs-target="#heroCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon"></span>
      </button>
      <button class="carousel-control-next" type="button" data-bs-target="#heroCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon"></span>
      </button>

    </div>
  </div>
</section>

<script>
  import { agregarAlCarrito } from "../js/carrito.js";

  document.addEventListener("astro:page-load", () => {
    document.querySelectorAll(".btn-add-cart").forEach((btn) => {
      btn.addEventListener("click", () => {
        const id = btn.getAttribute("data-id");
        const titulo = btn.getAttribute("data-titulo") || "";
        const precio = parseFloat(btn.getAttribute("data-precio") || "0");
        if (id) agregarAlCarrito(id, titulo, precio);
      });
    });
  });
</script>

<style is:global>
  .bev-carousel-custom {
    width: 100%;
    max-width: 100%;
    margin: 0;
    overflow: hidden;
    box-shadow: var(--shadow-md);
    border-radius: 0;
  }

  .bev-slide {
    height: 70vh;
    min-height: 300px;
    position: relative;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    background-color: var(--pr-gray-1000);
    display: flex;
    align-items: center;
  }

  .bev-slide-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to right,
      rgba(0, 0, 0, 0.85) 0%,
      rgba(0, 0, 0, 0.75) 35%,
      rgba(0, 0, 0, 0.75) 65%,
      rgba(0, 0, 0, 0.85) 100%
    );
  }

  .bev-slide-content {
    position: relative;
    z-index: 2;
    padding-left: 1rem;
    padding-right: 1rem;
    display: flex;
    flex-direction: column;
    align-items: center; /* centrado vertical y horizontal */
  }

  /* TEXTO MUCHO MÁS GRANDE, NOTORIO Y EN NEGRITA */
  .bev-eyebrow {
    font-size: 1.1rem;
    letter-spacing: 0.4em;
    text-transform: uppercase;
    color: var(--pr-white);
    opacity: 0.9;
    margin-bottom: 1rem;
    font-weight: 600;
  }

  .bev-slide-title {
    font-family: "Lato", sans-serif;
    font-size: clamp(2.8rem, 6vw, 5rem);
    font-weight: 800; /* mucho más negrita */
    color: var(--pr-white);
    margin-bottom: 0.4rem;
    line-height: 1.05;
  }

  .bev-slide-author {
    font-size: 1.35rem;
    font-style: italic;
    color: var(--pr-white);
    opacity: 0.95;
    margin-bottom: 1.2rem;
    font-weight: 700; /* negrita */
  }

  .bev-slide-desc {
    font-size: 1.25rem;
    color: var(--pr-white);
    opacity: 0.9;
    max-width: 720px;
    line-height: 1.4;
    font-weight: 500;
  }

  /* Botones también centrados */
  .d-flex.gap-3.flex-wrap.mt-4.justify-content-center {
    justify-content: center;
  }

  /* FLECHAS (sin cambios) */
  .bev-carousel-custom .carousel-control-prev,
  .bev-carousel-custom .carousel-control-next {
    width: 80px;
    height: 80px;
    top: 50%;
    transform: translateY(-50%);
    opacity: 1;
    background-color: rgba(0, 0, 0, 0.35);
    border-radius: 50%;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .bev-carousel-custom .carousel-control-prev:hover,
  .bev-carousel-custom .carousel-control-next:hover {
    background-color: rgba(0, 0, 0, 0.6);
    transform: translateY(-50%) scale(1.1);
  }

  .bev-carousel-custom .carousel-control-prev-icon,
  .bev-carousel-custom .carousel-control-next-icon {
    width: 48px;
    height: 48px;
    background-size: 100% 100%;
    filter: brightness(0) invert(1);
  }

  @media (max-width: 768px) {
    .bev-slide {
      height: 50vh; /* un poco más alto en móvil para que el texto grande respire */
    }

    .bev-slide-content {
      padding-left: 1rem;
      padding-right: 1rem;
    }

    .bev-slide-title {
      font-size: clamp(2.2rem, 7vw, 3.8rem);
    }

    .bev-slide-author {
      font-size: 1.15rem;
    }

    .bev-slide-desc {
      font-size: 1.1rem;
    }

    .bev-carousel-custom .carousel-control-prev,
    .bev-carousel-custom .carousel-control-next {
      width: 60px;
      height: 60px;
    }

    .bev-carousel-custom .carousel-control-prev-icon,
    .bev-carousel-custom .carousel-control-next-icon {
      width: 36px;
      height: 36px;
    }
  }
</style>